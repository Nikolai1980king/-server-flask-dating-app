# 🔍 Как увидеть расстояние в приложении

## 🚨 Проблема
Пользователь не видит, где выводится расстояние в приложении.

## ✅ Решение

### 1. **Где отображается расстояние**

Расстояние отображается в виде **уведомлений** в правом верхнем углу экрана при выборе заведения на карте.

### 2. **Как проверить работу уведомлений**

#### Шаг 1: Тест уведомлений
Откройте: `http://localhost:5000/test_notification`

Нажмите кнопки:
- ✅ **Успех** - зеленое уведомление
- ℹ️ **Информация** - синее уведомление  
- ❌ **Ошибка** - красное уведомление
- 🧭 **Рассчитать расстояние** - тест API с geopy

#### Шаг 2: Тест в основном приложении
Откройте: `http://localhost:5000/create`

1. **Определите местоположение**:
   - Нажмите "📍 Определить местоположение"
   - Должны появиться ваши координаты

2. **Выберите заведение на карте**:
   - Кликните на значок кафе на карте
   - Должно появиться уведомление с расстоянием

### 3. **Как выглядят уведомления**

```
┌─────────────────────────────────┐
│ ✅ Расстояние OK: 50.2м (geopy) │  ← Зеленое уведомление (≤100м)
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ⚠️ Расстояние: 456.7м (geopy)   │  ← Синее уведомление (>100м)
└─────────────────────────────────┘
```

### 4. **Где искать уведомления**

- **Позиция**: Правый верхний угол экрана
- **Время показа**: 5 секунд
- **Анимация**: Плавное появление справа

### 5. **Если уведомления не видны**

#### Проверьте:
1. **Браузер**: Убедитесь, что JavaScript включен
2. **Консоль**: Откройте F12 и проверьте ошибки
3. **Стили**: Убедитесь, что CSS загрузился

#### Возможные проблемы:
- **Блокировщик рекламы**: Может блокировать уведомления
- **Права доступа**: Браузер может блокировать уведомления
- **CSS не загрузился**: Проверьте стили `.notification`

### 6. **Альтернативные способы проверки**

#### Через консоль браузера:
```javascript
// Проверить функцию уведомлений
showNotification('Тест уведомления', 'success');

// Проверить API расстояния
fetch('/api/calculate-distance', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({
        lat1: 55.7558, lon1: 37.6176,
        lat2: 55.7517, lon2: 37.6178
    })
}).then(r => r.json()).then(console.log);
```

#### Через curl:
```bash
curl -X POST http://localhost:5000/api/calculate-distance \
  -H "Content-Type: application/json" \
  -d '{"lat1": 55.7558, "lon1": 37.6176, "lat2": 55.7517, "lon2": 37.6178}'
```

### 7. **Пошаговая диагностика**

#### Шаг 1: Проверьте тест уведомлений
```
http://localhost:5000/test_notification
```
- Должны появиться уведомления при нажатии кнопок

#### Шаг 2: Проверьте основной интерфейс
```
http://localhost:5000/create
```
- Определите местоположение
- Кликните на кафе на карте
- Должно появиться уведомление с расстоянием

#### Шаг 3: Проверьте консоль браузера
- Откройте F12 → Console
- Должны быть логи о расчете расстояния

### 8. **Ожидаемые сообщения в консоли**

```
🔄 Отправляем запрос к API для расчета расстояния...
📍 Координаты: (55.7558, 37.6176) -> (55.7517, 37.6178)
📡 Статус ответа API: 200
📊 Ответ API: {"success":true,"distance":456.66,"distance_meters":456.66,"distance_km":0.457}
✅ Расстояние рассчитано: 456.66 м
```

### 9. **Если ничего не работает**

1. **Перезапустите сервер**:
   ```bash
   # Остановите сервер (Ctrl+C)
   python3 app.py
   ```

2. **Очистите кэш браузера**:
   - Нажмите Ctrl+Shift+R (жесткая перезагрузка)

3. **Проверьте тест уведомлений**:
   - `http://localhost:5000/test_notification`

4. **Проверьте API**:
   - `http://localhost:5000/test_simple_distance`

### 10. **Структура уведомлений**

```css
.notification {
    position: fixed;
    top: 20px;
    right: 20px;
    padding: 15px 20px;
    border-radius: 10px;
    color: white;
    font-weight: bold;
    z-index: 1000;
    max-width: 400px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    animation: slideIn 0.3s ease;
}

.success { background: linear-gradient(90deg, #4CAF50 0%, #81c784 100%); }
.info { background: linear-gradient(90deg, #2196F3 0%, #64B5F6 100%); }
.error { background: linear-gradient(90deg, #f44336 0%, #e57373 100%); }
```

## 🎯 Итог

Расстояние отображается в виде **уведомлений в правом верхнем углу** при выборе заведения на карте. Если уведомления не видны, проверьте тест: `http://localhost:5000/test_notification` 