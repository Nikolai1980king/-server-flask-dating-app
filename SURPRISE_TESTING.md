# 🧪 Тестирование функции "Удивить"

## ✅ Быстрое тестирование

### Шаг 1: Миграция БД (если еще не выполнена)
```bash
cd /home/nikolai/PycharmProjects/flask_server
python migrate_add_sent_jokes.py
```

### Шаг 2: Запуск приложения
```bash
python app.py
```

### Шаг 3: Создание тестовых аккаунтов

Вам нужно **два аккаунта с мэтчем** для тестирования:

1. **Откройте браузер 1** (например, Chrome)
   - Перейдите на https://192.168.255.137
   - Создайте профиль A
   - Лайкните профиль B

2. **Откройте браузер 2** (например, Firefox или режим инкогнито)
   - Перейдите на https://192.168.255.137
   - Создайте профиль B
   - Лайкните профиль A
   - ✅ **МЭТЧ СОЗДАН!**

### Шаг 4: Тестирование кнопки "Удивить"

**В браузере 1 (профиль A):**
1. Перейдите на страницу "Посетители" (👥)
2. Найдите карточку профиля B
3. **Проверьте наличие кнопки ✨ "Удивить"** в правом нижнем углу карточки
   - ✅ Если есть - продолжайте
   - ❌ Если нет - проверьте наличие мэтча

### Шаг 5: Тестирование модального окна

1. Нажмите на кнопку ✨ "Удивить"
2. **Проверьте модальное окно:**
   - ✅ Появилось темное затемнение фона
   - ✅ Показана карточка с заголовком "Удивить собеседника"
   - ✅ Три опции: 🍰 Десерт, 🍾 Шампанское, 😄 Рассмешить
   - ✅ Крестик для закрытия в правом верхнем углу
3. Попробуйте закрыть окно кликом вне его
4. Снова откройте модальное окно

### Шаг 6: Тестирование десерта 🍰

1. Откройте модальное окно
2. Нажмите на "Подарить десерт"
3. **Проверьте:**
   - ✅ Уведомление "🍰 Десерт отправлен [Имя]!"
   - ✅ Модальное окно закрылось
4. **В браузере 2 (профиль B):**
   - Откройте "Мои сообщения" (✉️)
   - Откройте чат с профилем A
   - ✅ **Должно быть сообщение "🍰 SURPRISE_DESSERT"**

### Шаг 7: Тестирование шампанского 🍾

**САМАЯ ЗРЕЛИЩНАЯ ЧАСТЬ!**

1. В браузере 1, откройте модальное окно
2. Нажмите на "Шампанское"
3. **Проверьте анимацию:**
   - ✅ Появилась большая бутылка 🍾 в центре экрана
   - ✅ Пробка 🟤 вылетела вверх с вращением
   - ✅ Показались искры ✨💫✨
   - ✅ Анимация длится ~1 секунду
   - ✅ После анимации все исчезает
4. ✅ Уведомление "🍾 Шампанское отправлено [Имя]!"
5. **В браузере 2:**
   - Откройте чат
   - ✅ **Должно быть сообщение "🍾 SURPRISE_CHAMPAGNE"**

### Шаг 8: Тестирование анекдотов 😄

**Тест 1: Отправка первого анекдота**
1. Откройте модальное окно
2. Нажмите на "Рассмешить"
3. **Проверьте:**
   - ✅ Уведомление "😄 Анекдот отправлен [Имя]!"
4. **В браузере 2:**
   - Откройте чат
   - ✅ **Должен быть анекдот с маркером "😄 SURPRISE_JOKE"**
   - ✅ Текст анекдота про ресторан
   - Запомните этот анекдот!

**Тест 2: Отправка второго анекдота**
1. Отправьте еще один анекдот
2. **В браузере 2:**
   - ✅ Анекдот должен быть **ДРУГОЙ** (не тот же, что первый)

**Тест 3: Отправка всех 25 анекдотов**
1. Отправьте все 25 анекдотов (да, это долго 😅)
2. После 25-го анекдота попробуйте отправить еще один
3. **Проверьте:**
   - ℹ️ Уведомление "Все анекдоты уже отправлены [Имя]"
   - Модальное окно НЕ закрылось (можно выбрать другой вариант)

**Тест 4: Проверка уникальности для разных пар**
1. Создайте профиль C
2. Создайте мэтч между A и C
3. Отправьте анекдот от A к C
4. **Проверьте:**
   - ✅ Может прийти анекдот, который уже был отправлен от A к B
   - ✅ Анекдоты отслеживаются **для каждой пары отдельно**

### Шаг 9: Тестирование защиты

**Тест: Попытка отправить без мэтча**
1. Создайте профиль D (без мэтча с A)
2. В браузере 1, перейдите на "Посетители"
3. Найдите карточку профиля D
4. **Проверьте:**
   - ❌ Кнопка ✨ "Удивить" **НЕ ДОЛЖНА быть видна**
5. Если попытаться отправить через API напрямую (через консоль):
   ```javascript
   fetch('/api/send-surprise', {
       method: 'POST',
       headers: {'Content-Type': 'application/json'},
       body: JSON.stringify({receiver_id: 'ID_без_мэтча', type: 'dessert'})
   })
   ```
6. **Проверьте ответ:**
   - ❌ `{"error": "Сюрпризы можно отправлять только при наличии мэтча"}`
   - Статус: 403 Forbidden

## 🐛 Известные проблемы и решения

### Проблема 1: Кнопка "Удивить" не появляется
**Решение:**
- Проверьте наличие мэтча между пользователями
- Оба должны лайкнуть друг друга
- Обновите страницу

### Проблема 2: Анимация шампанского не показывается
**Решение:**
- Проверьте консоль браузера (F12)
- Анимация показывается локально у отправителя
- Длится всего 1 секунду - может не успеть заметить

### Проблема 3: Анекдоты повторяются
**Решение:**
- Проверьте, что миграция БД выполнена успешно
- Проверьте таблицу `sent_joke`: `python check_db.py`
- Очистите БД и создайте заново

### Проблема 4: Сообщения не приходят в чат
**Решение:**
- Проверьте подключение Socket.IO
- Проверьте консоль браузера на ошибки
- Обновите страницу чата

## 📊 Проверка базы данных

Для проверки отправленных анекдотов:

```python
import sqlite3

conn = sqlite3.connect('dating_app.db')
cursor = conn.cursor()

# Все отправленные анекдоты
cursor.execute("SELECT * FROM sent_joke")
for row in cursor.fetchall():
    print(f"От {row[1]} к {row[2]}: анекдот #{row[3]} в {row[4]}")

# Анекдоты от конкретного пользователя
cursor.execute("SELECT * FROM sent_joke WHERE sender_id = ?", ('USER_ID',))
print(cursor.fetchall())

conn.close()
```

## ✅ Чек-лист успешного тестирования

- [ ] Миграция БД выполнена успешно
- [ ] Приложение запущено без ошибок
- [ ] Создано 2+ профиля с мэтчами
- [ ] Кнопка ✨ "Удивить" видна для профилей с мэтчем
- [ ] Кнопка НЕ видна для профилей без мэтча
- [ ] Модальное окно открывается и закрывается
- [ ] Десерт 🍰 отправляется и получается
- [ ] Шампанское 🍾 показывает анимацию вылета пробки
- [ ] Анекдоты 😄 отправляются без повторов
- [ ] После 25 анекдотов показывается сообщение об исчерпании
- [ ] Уведомления работают корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах приложения

## 🎉 Итог

Если все пункты чек-листа выполнены - **функция работает идеально!** 🎊

Можете пользоваться и удивлять своих собеседников! ✨



























